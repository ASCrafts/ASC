[build]
  command = "vite build"
  publish = "."

[build.environment]
  NITRO_PRESET = "netlify"

# This catches all incoming page requests and forwards them to the SSR server engine
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200
  force = false